function characterSequence(params) {

    let input = params[0];

    for (let i = 0; i < input.length; i++) {

        console.log(input[i]);

    }
}

characterSequence(["softuni"]);

characterSequence(["ice cream"]);

characterSequence(["kurti kurtev"])

characterSequence(["baba yaga"])